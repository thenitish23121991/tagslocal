
var mongo = require('mongo');
var db = mongo.db;
var products = mongo.products;


module.exports = {

add_category : function(category){
var promise = products.update({'category':category});
promise.on('complete',function(err,docs){
console.log(docs);
});
},

find_by_category : function(category,callback){
var promise = products.find({'category':category});
promise.on('complete',function(err,docs){

});
}

}