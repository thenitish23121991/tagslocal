

var mongo = require('mongo');
var db = mongo.db;
var color = db.color;

module.exports = {

get_color_code : function(name,callback){
var promise = color.find({name:name});
promise.on('complete',function(err,docs){
callback(docs);
});
}


}